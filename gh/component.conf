# Metadata for the auto-discovered component CLI. The component name is the
# directory name; lower COMPONENT_ORDER sorts earlier in the menu.
COMPONENT_DESCRIPTION="GitHub CLI - install, auth, and config"
COMPONENT_ORDER=30
